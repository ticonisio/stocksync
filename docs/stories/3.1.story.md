# Story 3.1: Dashboard Principal — Lista de Produtos & Estoque

## Status
Draft

## Executor Assignment
```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "build", "test"]
```

## Story
**As a** lojista,
**I want** ver todos os meus produtos e variantes com seus níveis de estoque em uma única tela,
**so that** eu possa entender o estado completo do meu inventário em menos de 10 segundos.

## Acceptance Criteria
1. `/dashboard` exibe tabela paginada com todos os produtos sincronizados da store do usuário
2. Cada produto mostra rollup: nome, estoque disponível (soma variantes), reservado, comprometido
3. Produto expansível via clique para ver breakdown por variante (título, SKU, disponível, reservado, comprometido)
4. Estoque zero em todas as variantes exibe badge "Esgotado" na linha do produto
5. Paginação de 25 produtos por página com controles anterior/próximo e indicador de total
6. Carregamento inicial em ≤ 2s para lojas com até 500 produtos (Server Component com Prisma direto)
7. Estado vazio amigável quando nenhum produto sincronizado (com link para `/onboarding/connect-shopify`)

---

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Frontend (Dashboard Table) + Backend (API)
**Secondary Type(s)**: Data (Prisma queries com rollup)
**Complexity**: Medium

### Specialized Agent Assignment
**Primary Agents**:
- @dev: Implementação (API route, componentes de tabela, Server Component)
- @qa: Quality gate — validar rollup correto, paginação, estados de loading/empty

**Supporting Agents**:
- @qa: Validar performance (≤ 2s) e expansão de variantes

### Quality Gate Tasks
- [ ] Pre-Commit (@dev): lint + typecheck + build + test passa sem erros

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL only

**Predicted Behavior**:
- CRITICAL issues: auto_fix
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- Performance: query Prisma com `include variants` não deve fazer N+1; usar `findMany` com `include` único
- Auth: `getServerSession` + verificação de `store.userId === session.user.id` em cada query de inventário

**Secondary Focus**:
- UX: skeleton loading durante transições de página
- Acessibilidade: tabela com `role="table"`, aria-labels adequados

---

## Tasks / Subtasks

- [ ] **Task 1: Criar API route `GET /api/inventory`** (AC: 1, 2, 3, 5)
  - [ ] 1.1 Criar `src/app/api/inventory/route.ts` — GET com autenticação via `getServerSession`
  - [ ] 1.2 Query Prisma: buscar produtos com `include: { variants: true }` filtrado por `storeId` da store do usuário
  - [ ] 1.3 Suportar query params: `?page=1&limit=25` para paginação
  - [ ] 1.4 Response: `{ products: ProductWithVariants[], total: number, page: number, totalPages: number }`
  - [ ] 1.5 Calcular rollup de cada produto: `availableRollup`, `reservedRollup`, `committedRollup` (soma das variantes)
  - [ ] 1.6 Escrever testes em `tests/unit/inventory-route.test.ts`: 401 sem auth, retorna produtos paginados, rollup correto

- [ ] **Task 2: Criar componente de tabela de inventário** (AC: 2, 3, 4, 5)
  - [ ] 2.1 Criar `src/components/inventory/InventoryTable.tsx` — Client Component
  - [ ] 2.2 Colunas: Produto / SKU / Disponível / Reservado / Comprometido — com `<table>` acessível
  - [ ] 2.3 Linha de produto expansível via botão `ChevronRight`/`ChevronDown` (toggle local com `useState`)
  - [ ] 2.4 Sub-linhas de variantes exibidas quando produto expandido (indentação visual)
  - [ ] 2.5 Badge "Esgotado" (`availableRollup === 0`) com cor vermelha via shadcn/ui `Badge variant="destructive"`
  - [ ] 2.6 Controles de paginação: botões Anterior/Próximo + texto "Página X de Y" + total de produtos

- [ ] **Task 3: Refatorar página `/dashboard`** (AC: 1, 6, 7)
  - [ ] 3.1 Atualizar `src/app/(dashboard)/dashboard/page.tsx` — Server Component que faz fetch inicial via Prisma direto (não API)
  - [ ] 3.2 Buscar `products` com `include: { variants: true }` limitado a 25 por página via `searchParams.page`
  - [ ] 3.3 Passar dados como props para `<InventoryTable />` (hydration sem loading spinner no SSR)
  - [ ] 3.4 Estado vazio: quando `products.length === 0`, exibir mensagem + link para `/onboarding/connect-shopify`
  - [ ] 3.5 Loading skeleton: criar `src/app/(dashboard)/dashboard/loading.tsx` com skeleton de tabela

- [ ] **Task 4: Validações finais** (AC: todos)
  - [ ] 4.1 `npm run lint` — sem erros
  - [ ] 4.2 `npm run typecheck` — sem erros
  - [ ] 4.3 `npm run test` — todos os testes passam
  - [ ] 4.4 `npm run build` — build de produção sem erros
  - [ ] 4.5 Testar manualmente: verificar rollup correto, expandir/colapsar variantes, paginação

---

## Dev Notes

### Tech Stack Relevante
- **Next.js 14 Server Components:** fetch direto via Prisma no Server Component para TTFB ≤ 2s
- **Prisma `findMany` com `include`:** uma query; evitar N+1 com `include: { variants: true }`
- **shadcn/ui:** `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableCell`, `Badge`
- **lucide-react:** `ChevronRight`, `ChevronDown` para toggle de expansão

### Query Prisma para Dashboard
[Source: `docs/architecture.md` Section 4.3/4.4]

```typescript
// src/app/(dashboard)/dashboard/page.tsx
import { prisma } from '@/lib/prisma';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import { redirect } from 'next/navigation';

const ITEMS_PER_PAGE = 25;

export default async function DashboardPage({
  searchParams,
}: {
  searchParams: { page?: string };
}) {
  const session = await getServerSession(authOptions);
  if (!session) redirect('/login');

  const store = await prisma.store.findFirst({ where: { userId: session.user.id } });
  if (!store) redirect('/onboarding/connect-shopify');

  const page = Math.max(1, parseInt(searchParams.page ?? '1', 10));
  const skip = (page - 1) * ITEMS_PER_PAGE;

  const [products, total] = await prisma.$transaction([
    prisma.product.findMany({
      where: { storeId: store.id },
      include: { variants: true },
      skip,
      take: ITEMS_PER_PAGE,
      orderBy: { title: 'asc' },
    }),
    prisma.product.count({ where: { storeId: store.id } }),
  ]);

  return <InventoryTable products={products} total={total} page={page} perPage={ITEMS_PER_PAGE} />;
}
```

### Cálculo de Rollup por Produto

```typescript
function calcRollup(variants: Variant[]) {
  return variants.reduce(
    (acc, v) => ({
      availableRollup: acc.availableRollup + v.availableStock,
      reservedRollup: acc.reservedRollup + v.reservedStock,
      committedRollup: acc.committedRollup + v.committedStock,
    }),
    { availableRollup: 0, reservedRollup: 0, committedRollup: 0 }
  );
}
```

### Tipos TypeScript
```typescript
// src/types/inventory.ts
export type VariantWithStock = {
  id: string;
  title: string;
  sku: string | null;
  availableStock: number;
  reservedStock: number;
  committedStock: number;
};

export type ProductWithVariants = {
  id: string;
  title: string;
  variants: VariantWithStock[];
};
```

### Decisões de Arquitetura (não reabrir)
- Server Component para fetch inicial elimina loading spinner no SSR (melhor TTFB)
- API route `GET /api/inventory` criada para uso futuro pelo Realtime client (Story 3.2 usará para re-fetch)
- Paginação via `searchParams` do Server Component (sem estado client-side) — URL navegável
- `prisma.$transaction` para `findMany` + `count` em uma única roundtrip ao banco

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 1.0 | Story criada pelo @sm (River) | River (SM) |

---

## Dev Agent Record
*(Preenchido pelo @dev durante implementação)*

### Agent Model Used
_TBD_

### Debug Log References
_TBD_

### Completion Notes List
_TBD_

### File List
_TBD_

---

## QA Results
*(Preenchido pelo @qa após implementação)*

_TBD_
