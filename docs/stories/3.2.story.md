# Story 3.2: Atualização de Estoque em Tempo Real

## Status
Draft

## Executor Assignment
```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "build", "test"]
```

## Story
**As a** lojista,
**I want** que o dashboard atualize automaticamente quando um pedido entra ou muda de status,
**so that** eu sempre veja o estoque real sem precisar recarregar a página.

## Acceptance Criteria
1. Dashboard se inscreve no canal Supabase Realtime ao montar, desinscreve ao desmontar
2. Quando variante atualizada no banco (via webhook), linha do produto/variante atualiza em ≤ 5s sem reload
3. Atualização não intrusiva — sem piscar ou perder posição de scroll
4. Linha atualizada recebe highlight temporário (2s) para indicar mudança
5. Reconexão automática se canal Realtime cair, com toast discreto "Reconectando..."
6. Re-fetch via `GET /api/inventory` ao reconectar para garantir estado consistente
7. Zustand store gerencia estado de inventário para sincronização entre componentes

---

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Frontend (Real-time / WebSocket)
**Secondary Type(s)**: State Management (Zustand), Infrastructure (Supabase Realtime)
**Complexity**: Medium-High

### Specialized Agent Assignment
**Primary Agents**:
- @dev: Implementação (Supabase Realtime client, Zustand store, highlight animation)
- @qa: Quality gate — validar reconexão, estado consistente, sem memory leaks

**Supporting Agents**:
- @qa: Validar que subscription é destruída no unmount (sem memory leak)

### Quality Gate Tasks
- [ ] Pre-Commit (@dev): lint + typecheck + build + test passa sem erros

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL only

**Predicted Behavior**:
- CRITICAL issues: auto_fix (memory leaks, subscription não destruída)
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- Memory: `useEffect` cleanup destrói subscription (`channel.unsubscribe()`)
- Security: Supabase Realtime filtra por `storeId` — usuário só recebe eventos da própria store
- State: Zustand store atualiza imutavelmente (não mutar estado diretamente)

**Secondary Focus**:
- UX: highlight animation usa CSS transition, não re-render forçado
- Error handling: canal `CHANNEL_ERROR` exibe toast e tenta reconectar

---

## Tasks / Subtasks

- [ ] **Task 1: Instalar e configurar Supabase client** (AC: 1)
  - [ ] 1.1 Instalar `@supabase/supabase-js` — `npm install @supabase/supabase-js`
  - [ ] 1.2 Criar `src/lib/supabase.ts` — client singleton com `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - [ ] 1.3 Adicionar variáveis ao `.env.example`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - [ ] 1.4 Habilitar Realtime no Supabase Dashboard para a tabela `variants` (instruções no Dev Notes)

- [ ] **Task 2: Criar Zustand store de inventário** (AC: 7)
  - [ ] 2.1 Criar `src/store/inventoryStore.ts` com Zustand
  - [ ] 2.2 State: `{ products: ProductWithVariants[], total: number, updatedVariantIds: Set<string> }`
  - [ ] 2.3 Actions: `setProducts`, `updateVariant(variantId, patch)`, `markUpdated(variantId)`, `clearUpdated(variantId)`
  - [ ] 2.4 `updateVariant` recalcula rollup do produto pai automaticamente

- [ ] **Task 3: Hook `useInventoryRealtime`** (AC: 1, 2, 5, 6)
  - [ ] 3.1 Criar `src/hooks/useInventoryRealtime.ts` — hook de subscrição Supabase Realtime
  - [ ] 3.2 Subscrever ao canal `inventory:{storeId}` filtrando `table: 'variants'`
  - [ ] 3.3 Ao receber `UPDATE`: chamar `updateVariant` no Zustand store com os novos valores
  - [ ] 3.4 Handler de `CHANNEL_ERROR`/`CLOSED`: mostrar toast "Reconectando..." + re-fetch + re-subscribe após 3s
  - [ ] 3.5 `useEffect` cleanup: `channel.unsubscribe()` ao desmontar componente
  - [ ] 3.6 Re-fetch `GET /api/inventory` via SWR ao reconectar para garantir consistência

- [ ] **Task 4: Integrar Realtime no InventoryTable** (AC: 2, 3, 4)
  - [ ] 4.1 Converter `InventoryTable` para ler do Zustand store (inicializar com props SSR)
  - [ ] 4.2 Chamar `useInventoryRealtime(storeId)` no topo do componente
  - [ ] 4.3 Adicionar classe CSS `highlight-flash` em linhas cujo `variantId` está em `updatedVariantIds`
  - [ ] 4.4 Após 2s, chamar `clearUpdated(variantId)` para remover highlight
  - [ ] 4.5 CSS: `@keyframes flash { 0%, 100% { background: transparent; } 50% { background: hsl(var(--accent)); } }`

- [ ] **Task 5: Validações finais** (AC: todos)
  - [ ] 5.1 `npm run lint` — sem erros
  - [ ] 5.2 `npm run typecheck` — sem erros
  - [ ] 5.3 `npm run test` — todos os testes passam
  - [ ] 5.4 `npm run build` — build de produção sem erros
  - [ ] 5.5 Testar manualmente: criar pedido na Shopify, verificar update no dashboard em ≤ 5s

---

## Dev Notes

### Tech Stack Relevante
- **@supabase/supabase-js 2.x:** Realtime subscriptions via Postgres CDC
- **Zustand 4.x:** Estado global client-side para inventário
- **SWR 2.x:** Cache + revalidação para re-fetch ao reconectar
- **Supabase Realtime:** CDC direto do PostgreSQL — sem código extra no servidor
[Source: `docs/architecture.md` Section 2.5]

### Configuração Supabase Client
```typescript
// src/lib/supabase.ts
import { createClient } from '@supabase/supabase-js';

export const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);
```

### Variáveis de Ambiente Necessárias
```env
NEXT_PUBLIC_SUPABASE_URL="https://xxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."
```
Disponíveis em: Supabase Dashboard → Project Settings → API.

### Habilitar Realtime na Tabela `variants`
No Supabase Dashboard:
1. Database → Replication
2. Adicionar tabela `variants` ao canal de replication
3. Ou via SQL: `ALTER TABLE variants REPLICA IDENTITY FULL;`

### Hook useInventoryRealtime
```typescript
// src/hooks/useInventoryRealtime.ts
import { useEffect } from 'react';
import { supabase } from '@/lib/supabase';
import { useInventoryStore } from '@/store/inventoryStore';
import { toast } from 'sonner';

export function useInventoryRealtime(storeId: string) {
  const { updateVariant, markUpdated } = useInventoryStore();

  useEffect(() => {
    const channel = supabase
      .channel(`inventory:${storeId}`)
      .on(
        'postgres_changes',
        { event: 'UPDATE', schema: 'public', table: 'variants', filter: `storeId=eq.${storeId}` },
        (payload) => {
          const v = payload.new as { id: string; availableStock: number; reservedStock: number; committedStock: number };
          updateVariant(v.id, {
            availableStock: v.availableStock,
            reservedStock: v.reservedStock,
            committedStock: v.committedStock,
          });
          markUpdated(v.id);
          setTimeout(() => useInventoryStore.getState().clearUpdated(v.id), 2000);
        }
      )
      .subscribe((status) => {
        if (status === 'CHANNEL_ERROR' || status === 'CLOSED') {
          toast.error('Conexão perdida. Reconectando...');
        }
      });

    return () => { void channel.unsubscribe(); };
  }, [storeId, updateVariant, markUpdated]);
}
```

### Zustand Store
```typescript
// src/store/inventoryStore.ts
import { create } from 'zustand';
import type { ProductWithVariants } from '@/types/inventory';

interface InventoryState {
  products: ProductWithVariants[];
  total: number;
  updatedVariantIds: Set<string>;
  setProducts: (products: ProductWithVariants[], total: number) => void;
  updateVariant: (variantId: string, patch: Partial<VariantWithStock>) => void;
  markUpdated: (variantId: string) => void;
  clearUpdated: (variantId: string) => void;
}

export const useInventoryStore = create<InventoryState>((set) => ({
  products: [],
  total: 0,
  updatedVariantIds: new Set(),
  setProducts: (products, total) => set({ products, total }),
  updateVariant: (variantId, patch) =>
    set((state) => ({
      products: state.products.map((p) => ({
        ...p,
        variants: p.variants.map((v) => v.id === variantId ? { ...v, ...patch } : v),
      })),
    })),
  markUpdated: (variantId) =>
    set((state) => ({ updatedVariantIds: new Set([...state.updatedVariantIds, variantId]) })),
  clearUpdated: (variantId) =>
    set((state) => {
      const next = new Set(state.updatedVariantIds);
      next.delete(variantId);
      return { updatedVariantIds: next };
    }),
}));
```

### Dependências a Instalar
```bash
npm install @supabase/supabase-js zustand swr sonner
```
(`sonner` para toast notifications — já comum em projetos shadcn/ui)

### Decisões de Arquitetura (não reabrir)
- Supabase Realtime via Postgres CDC — zero infraestrutura adicional no servidor (arquitetura docs)
- Zustand (não Context) para estado de inventário — evita re-renders em árvore grande
- Re-fetch ao reconectar via SWR garante que estado está consistente após downtime
- `storeId` como filtro no canal Realtime — usuário só recebe atualizações da própria loja

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 1.0 | Story criada pelo @sm (River) | River (SM) |

---

## Dev Agent Record
*(Preenchido pelo @dev durante implementação)*

### Agent Model Used
_TBD_

### Debug Log References
_TBD_

### Completion Notes List
_TBD_

### File List
_TBD_

---

## QA Results
*(Preenchido pelo @qa após implementação)*

_TBD_
